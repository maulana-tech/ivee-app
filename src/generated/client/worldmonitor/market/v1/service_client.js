// @ts-nocheck
// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/market/v1/service.proto
export class ValidationError extends Error {
    constructor(violations) {
        super("Validation failed");
        Object.defineProperty(this, "violations", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.name = "ValidationError";
        this.violations = violations;
    }
}
export class ApiError extends Error {
    constructor(statusCode, message, body) {
        super(message);
        Object.defineProperty(this, "statusCode", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "body", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.name = "ApiError";
        this.statusCode = statusCode;
        this.body = body;
    }
}
export class MarketServiceClient {
    constructor(baseURL, options) {
        Object.defineProperty(this, "baseURL", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "fetchFn", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "defaultHeaders", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.baseURL = baseURL.replace(/\/+$/, "");
        this.fetchFn = options?.fetch ?? globalThis.fetch;
        this.defaultHeaders = { ...options?.defaultHeaders };
    }
    async listMarketQuotes(req, options) {
        let path = "/api/market/v1/list-market-quotes";
        const params = new URLSearchParams();
        if (req.symbols != null && req.symbols !== "")
            params.set("symbols", String(req.symbols));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listCryptoQuotes(req, options) {
        let path = "/api/market/v1/list-crypto-quotes";
        const params = new URLSearchParams();
        if (req.ids != null && req.ids !== "")
            params.set("ids", String(req.ids));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listCommodityQuotes(req, options) {
        let path = "/api/market/v1/list-commodity-quotes";
        const params = new URLSearchParams();
        if (req.symbols != null && req.symbols !== "")
            params.set("symbols", String(req.symbols));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async getSectorSummary(req, options) {
        let path = "/api/market/v1/get-sector-summary";
        const params = new URLSearchParams();
        if (req.period != null && req.period !== "")
            params.set("period", String(req.period));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listStablecoinMarkets(req, options) {
        let path = "/api/market/v1/list-stablecoin-markets";
        const params = new URLSearchParams();
        if (req.coins != null && req.coins !== "")
            params.set("coins", String(req.coins));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listEtfFlows(req, options) {
        let path = "/api/market/v1/list-etf-flows";
        const url = this.baseURL + path;
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async getCountryStockIndex(req, options) {
        let path = "/api/market/v1/get-country-stock-index";
        const params = new URLSearchParams();
        if (req.countryCode != null && req.countryCode !== "")
            params.set("country_code", String(req.countryCode));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listGulfQuotes(req, options) {
        let path = "/api/market/v1/list-gulf-quotes";
        const url = this.baseURL + path;
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async analyzeStock(req, options) {
        let path = "/api/market/v1/analyze-stock";
        const params = new URLSearchParams();
        if (req.symbol != null && req.symbol !== "")
            params.set("symbol", String(req.symbol));
        if (req.name != null && req.name !== "")
            params.set("name", String(req.name));
        if (req.includeNews)
            params.set("include_news", String(req.includeNews));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async getStockAnalysisHistory(req, options) {
        let path = "/api/market/v1/get-stock-analysis-history";
        const params = new URLSearchParams();
        if (req.symbols != null && req.symbols !== "")
            params.set("symbols", String(req.symbols));
        if (req.limitPerSymbol != null && req.limitPerSymbol !== 0)
            params.set("limit_per_symbol", String(req.limitPerSymbol));
        if (req.includeNews)
            params.set("include_news", String(req.includeNews));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async backtestStock(req, options) {
        let path = "/api/market/v1/backtest-stock";
        const params = new URLSearchParams();
        if (req.symbol != null && req.symbol !== "")
            params.set("symbol", String(req.symbol));
        if (req.name != null && req.name !== "")
            params.set("name", String(req.name));
        if (req.evalWindowDays != null && req.evalWindowDays !== 0)
            params.set("eval_window_days", String(req.evalWindowDays));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listStoredStockBacktests(req, options) {
        let path = "/api/market/v1/list-stored-stock-backtests";
        const params = new URLSearchParams();
        if (req.symbols != null && req.symbols !== "")
            params.set("symbols", String(req.symbols));
        if (req.evalWindowDays != null && req.evalWindowDays !== 0)
            params.set("eval_window_days", String(req.evalWindowDays));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listCryptoSectors(req, options) {
        let path = "/api/market/v1/list-crypto-sectors";
        const url = this.baseURL + path;
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listDefiTokens(req, options) {
        let path = "/api/market/v1/list-defi-tokens";
        const url = this.baseURL + path;
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listAiTokens(req, options) {
        let path = "/api/market/v1/list-ai-tokens";
        const url = this.baseURL + path;
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listOtherTokens(req, options) {
        let path = "/api/market/v1/list-other-tokens";
        const url = this.baseURL + path;
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async getFearGreedIndex(req, options) {
        let path = "/api/market/v1/get-fear-greed-index";
        const url = this.baseURL + path;
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async listEarningsCalendar(req, options) {
        let path = "/api/market/v1/list-earnings-calendar";
        const params = new URLSearchParams();
        if (req.fromDate != null && req.fromDate !== "")
            params.set("fromDate", String(req.fromDate));
        if (req.toDate != null && req.toDate !== "")
            params.set("toDate", String(req.toDate));
        const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async getCotPositioning(req, options) {
        let path = "/api/market/v1/get-cot-positioning";
        const url = this.baseURL + path;
        const headers = {
            "Content-Type": "application/json",
            ...this.defaultHeaders,
            ...options?.headers,
        };
        const resp = await this.fetchFn(url, {
            method: "GET",
            headers,
            signal: options?.signal,
        });
        if (!resp.ok) {
            return this.handleError(resp);
        }
        return await resp.json();
    }
    async handleError(resp) {
        const body = await resp.text();
        if (resp.status === 400) {
            try {
                const parsed = JSON.parse(body);
                if (parsed.violations) {
                    throw new ValidationError(parsed.violations);
                }
            }
            catch (e) {
                if (e instanceof ValidationError)
                    throw e;
            }
        }
        throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
    }
}
